## What

<!-- One or two lines. Link the issue or the plan phase this closes. -->

## Checklist

- [ ] `pre-commit run --all-files` passes
- [ ] `npm run lint && npm test && npm run build` pass
- [ ] Tests cover the changed logic (the phase's check in `IMPLEMENTATION_PLAN.md`)
- [ ] Public API changes are reflected in `README.md` §3
